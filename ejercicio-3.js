// ? #3 = ** Encuentra la segunda palabra de un string **

const secondWord = "Hola, me llamo Erik";

function findSecondWord(text) {
    return text.split(" ")[1];
};

// console.log(findSecondWord(secondWord));