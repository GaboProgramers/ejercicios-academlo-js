// ? #5 = ** Obtén el correo de un usuario **

const setEmail = { name: "Erik", email: "erik@academlo.com" };

function getEmail(user) {

    const email = user.email;

    return email;
};

// console.log(getEmail(setEmail));